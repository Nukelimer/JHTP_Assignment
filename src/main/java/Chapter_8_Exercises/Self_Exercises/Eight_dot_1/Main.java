package Chapter_8_Exercises.Self_Exercises.Eight_dot_1;

import java.util.concurrent.ExecutionException;

public class Main
{


/*Fill in the blanks in each of the following statements:
a) A(n)_____imports all static members of a class.[STATIC IMPORT ON DEMAND].

b) String class static method_______is similar to method System.out.printf, but returns
 a formatted String rather than displaying a String in a command window.[FORMAT].

c) If a method contains a local variable with the same name as one of its class’s fields, the
local variable_______the field in that method’s scope.[SHADOW]

d) The public methods of a class are also known as the class’s ______ or ______.[INTERFACE OR PUBLIC INTERFACE]

e) A(n)________declaration imports one static member.[SINGLE STATIC IMPORT]

f) If a class declares constructors, the compiler will not create a(n)____.[DEFAULT CONSTRUCTOR]

g) An object’s______method is called implicitly when an object appears in code where
a String is needed.[TOSTRING() METHOD]

h) Get methods are commonly called____or_____[ACCESSOR or QUERY METHODS]

i) A(n)___________method tests whether a condition is true or false.[BOOLEAN RETURNING METHOD OR PREDICATE]

j) For every enum, the compiler generates a static method called________that returns
an array of the enum’s constants in the order in which they were declared.[VALUES()]

k) Composition is sometimes referred to as a(n)_______relationship.['HAS-A'].

l) A(n)___________declaration contains a comma-separated list of constants[ENUM].

m) A(n)________________variable represents classwide information that’s shared by all the objects
of the class.[STATIC VARIABLE]

n) A(n)_______________declaration imports one static member.[SINGLE STATIC IMPORT]

o) The______states that code should be granted only the amount of privilege and ac-
cess that it needs to accomplish its designated task.[PRINCIPLE OF LEAST PRIVILEGE].

p) Keyword_____specifies that a variable is not modifiable after initialization in a
 declaration or constructor.[FINAL]

q) A(n)_______imports all static members of a class.[STATIC IMPORT ON DEMAND].

r) Set methods are commonly called______because they typically change a value.[MUTATOR METHODS].

s) Use class______to perform precise monetary calculations.[BIGDECIMAL]

t) Use the________statement to indicate that a problem has occurred.[THROW STATEMENT]
*/


}
